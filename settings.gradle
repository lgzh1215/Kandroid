include ':app', ':core', ':win'

findProject(':app')?.with {
    name = 'android'
    buildFileName = 'android.gradle'
}

findProject(':core')?.with {
    name = 'kcsapi'
    buildFileName = 'core.gradle'
}

findProject(':win')?.with {
    name = 'windows'
    buildFileName = 'win.gradle'
}